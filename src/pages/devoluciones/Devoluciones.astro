---
import MainLayout from '../../layouts/lading/MainLayout.astro';
import { Image } from 'astro:assets';
import returnImage from '../../assets/img/landing/returns.png'; // Asegúrate de tener esta imagen o cámbiala por una que tengas

// Información de devoluciones
const returnsInfo = {
  title: "Política de Devoluciones",
  description: "Queremos que estés completamente satisfecho con tu compra. Conoce nuestra política de devoluciones y cambios.",
  returnPeriod: 30,
  conditions: [
    "El producto debe estar en perfectas condiciones y sin uso",
    "Debe conservar todas las etiquetas y embalaje original",
    "Es necesario presentar el comprobante de compra",
    "Los productos personalizados no admiten devolución"
  ],
  returnMethods: [
    { method: "En tienda", timeframe: "Inmediato", cost: "Gratis" },
    { method: "Por mensajería", timeframe: "7-10 días", cost: "$200" }
  ]
};
---

<MainLayout title="Política de Devoluciones">
  <section class="py-12 px-4 bg-gray-50">
    <div class="max-w-6xl mx-auto">
      <h1 class="text-3xl font-bold text-center mb-8 text-gray-800">{returnsInfo.title}</h1>

      <!-- Card principal de devoluciones (90% del contenido) -->
      <div class="bg-white rounded-lg shadow-lg overflow-hidden w-11/12 mx-auto">
        <div class="flex flex-col md:flex-row-reverse">
          <!-- Imagen de devoluciones (lado derecho en desktop, arriba en móvil) -->
          <div class="md:w-2/5 bg-indigo-50 flex items-center justify-center p-6">
            <Image
              src={returnImage}
              alt="Devoluciones y cambios"
              width={400}
              height={300}
              class="rounded-lg object-cover"
            />
          </div>

          <!-- Contenido de devoluciones (lado izquierdo en desktop, abajo en móvil) -->
          <div class="md:w-3/5 p-6 md:p-8">
            <div class="mb-6">
              <div class="inline-block px-3 py-1 bg-indigo-100 text-indigo-800 rounded-full text-sm font-medium mb-3">
                {returnsInfo.returnPeriod} días de garantía
              </div>
              <h2 class="text-2xl font-semibold text-gray-800 mb-3">Cambios y Devoluciones</h2>
              <p class="text-gray-600">{returnsInfo.description}</p>
            </div>

            <div class="mb-6">
              <h3 class="text-lg font-semibold text-gray-700 mb-3">Condiciones</h3>
              <ul class="space-y-2">
                {returnsInfo.conditions.map(condition => (
                  <li class="flex items-start">
                    <svg class="h-5 w-5 text-indigo-500 mr-2 mt-0.5" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                      <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M5 13l4 4L19 7"></path>
                    </svg>
                    <span class="text-gray-600">{condition}</span>
                  </li>
                ))}
              </ul>
            </div>

            <div class="mb-6">
              <h3 class="text-lg font-semibold text-gray-700 mb-3">Métodos de Devolución</h3>
              <div class="bg-gray-50 rounded-lg overflow-hidden">
                <div class="grid grid-cols-3 bg-indigo-50 p-3 text-sm font-medium text-gray-700">
                  <div>Método</div>
                  <div>Tiempo de procesamiento</div>
                  <div>Costo</div>
                </div>
                {returnsInfo.returnMethods.map((method, index) => (
                  <div class={`grid grid-cols-3 p-3 text-sm ${index % 2 === 0 ? 'bg-white' : 'bg-gray-50'}`}>
                    <div class="font-medium text-gray-800">{method.method}</div>
                    <div class="text-gray-600">{method.timeframe}</div>
                    <div class="text-gray-600">{method.cost}</div>
                  </div>
                ))}
              </div>
            </div>
          </div>
        </div>
      </div>

      <!-- Pasos para hacer una devolución -->
      <div class="mt-10 px-4">
        <h2 class="text-2xl font-semibold text-center mb-6 text-gray-800">¿Cómo hacer una devolución?</h2>
        <div class="max-w-4xl mx-auto">
          <div class="grid md:grid-cols-3 gap-6">
            <div class="bg-white rounded-lg shadow-md p-6 text-center">
              <div class="w-16 h-16 bg-indigo-100 rounded-full flex items-center justify-center mx-auto mb-4">
                <svg xmlns="http://www.w3.org/2000/svg" class="h-8 w-8 text-indigo-500" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M3 8l7.89 5.26a2 2 0 002.22 0L21 8M5 19h14a2 2 0 002-2V7a2 2 0 00-2-2H5a2 2 0 00-2 2v10a2 2 0 002 2z" />
                </svg>
              </div>
              <h3 class="font-semibold text-lg mb-2">1. Contáctanos</h3>
              <p class="text-gray-600 text-sm">Envía un email a soporte@tienda.com con tu número de pedido y motivo de devolución</p>
            </div>

            <div class="bg-white rounded-lg shadow-md p-6 text-center">
              <div class="w-16 h-16 bg-indigo-100 rounded-full flex items-center justify-center mx-auto mb-4">
                <svg xmlns="http://www.w3.org/2000/svg" class="h-8 w-8 text-indigo-500" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 12l2 2 4-4m6 2a9 9 0 11-18 0 9 9 0 0118 0z" />
                </svg>
              </div>
              <h3 class="font-semibold text-lg mb-2">2. Aprobación</h3>
              <p class="text-gray-600 text-sm">Recibirás un email con la confirmación y las instrucciones para proceder</p>
            </div>

            <div class="bg-white rounded-lg shadow-md p-6 text-center">
              <div class="w-16 h-16 bg-indigo-100 rounded-full flex items-center justify-center mx-auto mb-4">
                <svg xmlns="http://www.w3.org/2000/svg" class="h-8 w-8 text-indigo-500" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M16 15v-1a4 4 0 00-4-4H8m0 0l3 3m-3-3l3-3m9 14V5a2 2 0 00-2-2H6a2 2 0 00-2 2v16l4-2 4 2 4-2 4 2z" />
                </svg>
              </div>
              <h3 class="font-semibold text-lg mb-2">3. Reembolso</h3>
              <p class="text-gray-600 text-sm">Una vez recibido y verificado el producto, procesaremos tu reembolso</p>
            </div>
          </div>
        </div>
      </div>
    </div>
  </section>
</MainLayout>
