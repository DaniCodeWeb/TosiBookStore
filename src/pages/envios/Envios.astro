---
import MainLayout from '../../layouts/lading/MainLayout.astro';
import { Image } from 'astro:assets';
import deliveryImage from '../../assets/img/landing/delivery.png'; // Asegúrate de tener esta imagen o cámbiala por una que tengas

// Información de envíos
const shippingInfo = {
  title: "Información de Envíos",
  description: "Toda la información que necesitas saber sobre nuestros envíos y tiempos de entrega.",
  deliveryTimes: [
    { region: "Ciudad Principal", time: "24-48 horas" },
    { region: "Ciudades Secundarias", time: "2-3 días" },
    { region: "Zonas Rurales", time: "3-5 días" }
  ],
  minOrderForFreeShipping: 5000,
  trackingInfo: "Todos nuestros envíos incluyen número de seguimiento que recibirás por email"
};
---

<MainLayout title="Información de Envíos">
  <section class="py-12 px-4 bg-gray-50">
    <div class="max-w-6xl mx-auto">
      <h1 class="text-3xl font-bold text-center mb-8 text-gray-800">{shippingInfo.title}</h1>

      <!-- Card principal de envíos (90% del contenido) -->
      <div class="bg-white rounded-lg shadow-lg overflow-hidden w-11/12 mx-auto">
        <div class="md:flex">
          <!-- Imagen de envío (lado izquierdo en desktop, arriba en móvil) -->
          <div class="md:w-1/3 bg-blue-50 flex items-center justify-center p-6">
            <Image
              src={deliveryImage}
              alt="Envíos y entregas"
              width={400}
              height={300}
              class="rounded-lg object-cover"
            />
          </div>

          <!-- Contenido de envíos (lado derecho en desktop, abajo en móvil) -->
          <div class="md:w-2/3 p-6 md:p-8">
            <h2 class="text-2xl font-semibold text-gray-800 mb-4">Envíos a Todo el País</h2>
            <p class="text-gray-600 mb-6">{shippingInfo.description}</p>

            <div class="mb-6">
              <h3 class="text-lg font-semibold text-gray-700 mb-3">Tiempos de Entrega</h3>
              <div class="space-y-2">
                {shippingInfo.deliveryTimes.map(item => (
                  <div class="flex justify-between border-b border-gray-100 pb-2">
                    <span class="text-gray-700">{item.region}</span>
                    <span class="font-medium text-blue-600">{item.time}</span>
                  </div>
                ))}
              </div>
            </div>

            <div class="bg-blue-50 p-4 rounded-lg mb-6">
              <div class="flex items-center">
                <div class="mr-4 text-blue-500">
                  <svg xmlns="http://www.w3.org/2000/svg" class="h-8 w-8" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M13 16h-1v-4h-1m1-4h.01M21 12a9 9 0 11-18 0 9 9 0 0118 0z" />
                  </svg>
                </div>
                <div>
                  <h4 class="font-semibold text-gray-800">Envío Gratis</h4>
                  <p class="text-gray-600">En compras superiores a ${shippingInfo.minOrderForFreeShipping}</p>
                </div>
              </div>
            </div>

            <div class="flex items-center text-gray-700">
              <svg xmlns="http://www.w3.org/2000/svg" class="h-5 w-5 mr-2 text-blue-500" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 12l2 2 4-4m6 2a9 9 0 11-18 0 9 9 0 0118 0z" />
              </svg>
              <p>{shippingInfo.trackingInfo}</p>
            </div>
          </div>
        </div>
      </div>

      <!-- Información adicional -->
      <div class="mt-10 px-4">
        <h2 class="text-2xl font-semibold text-center mb-6 text-gray-800">¿Cómo rastrear tu pedido?</h2>
        <div class="max-w-3xl mx-auto">
          <div class="bg-white rounded-lg shadow-md p-6">
            <ol class="space-y-4">
              <li class="flex items-start">
                <div class="bg-blue-500 text-white rounded-full w-6 h-6 flex items-center justify-center mr-3 flex-shrink-0 mt-1">1</div>
                <p class="text-gray-700">Revisa tu correo electrónico para encontrar el número de seguimiento que te enviamos cuando tu pedido fue despachado.</p>
              </li>
              <li class="flex items-start">
                <div class="bg-blue-500 text-white rounded-full w-6 h-6 flex items-center justify-center mr-3 flex-shrink-0 mt-1">2</div>
                <p class="text-gray-700">Visita la sección "Mis Pedidos" en tu cuenta o haz clic en el enlace de seguimiento en el correo.</p>
              </li>
              <li class="flex items-start">
                <div class="bg-blue-500 text-white rounded-full w-6 h-6 flex items-center justify-center mr-3 flex-shrink-0 mt-1">3</div>
                <p class="text-gray-700">Ingresa el número de seguimiento para ver el estado actual y la ubicación de tu pedido.</p>
              </li>
            </ol>
          </div>
        </div>
      </div>
    </div>
  </section>
</MainLayout>
